/// Legacy import path — kept so existing code keeps compiling.
///
/// The implementation moved to `avogadro_bridge.dart` (platform-selected) and
/// `avogadro_interchange.dart` (format writers).
library;

export 'avogadro_bridge.dart';
